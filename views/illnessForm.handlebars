<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>

    <style>
        body {
            font-family: 'Lato', Helvetica, sans-serif;
        }

        form {
            
        }
        form h1 {
            background-color: #F7F7F7;
            border: 1px solid #293745;
            color: #293745;
            padding: 10px;
            text-align: center;
        }
        form fieldset {
            background-color: #293745;
            border: none;
            border-radius: 10px;
            color: #fff;
            padding: 30px 60px;
        }
        form fieldset legend {
            box-shadow: 0 0 8px #fff;
            color: #fff;
            padding: 0 15px;
            background-color: #2F9ED0;
            border-radius: 10px;
        }
        form fieldset legend h4 {
            font-weight: lighter;
        }
        form fieldset button {
            background-color: #FDCE4B;
            color: #293745;
            border: none;
            border-radius: 10px;
            padding: 15px 25px;
        }
        form fieldset button:hover {
            opacity: 0.8;
        }

        ul {
            list-style-type: none;
            background-color: #293745;
            margin: 0;
            padding: 0;
            height: 80px;
            display: flex;
            align-items: center;
            display: fixed;
        }

        ul li {
            margin: 30px;
        }

        ul li a {
            text-decoration: none;
            color: #fff;
            margin: 10px;
        }

        ul li a:hover {
            text-decoration: underline;
        }

        .symptoms-container {
            display: flex;
            justify-content: center;
            align-items: center;
        }

        .symptoms-container input,
        span {
            float: right;
        }

        label[for="yes"],
        label[for="no"] {
            margin: 10px 5px;
            display: inline-block;
        }
        .symptom {
            margin: 10px;
        }
    </style>
</head>

<body>
    <div class="container">
        <ul>
            <li><a href="/">HOME</a></li>
            <li> <a href="/goToForm">CHECK SYMPTOMS</a> </li>
            <li> <a href="/chart">MAKE APPOINTMENT</a> </li>
            <li> <a href="/information">LEARN MORE</a> </li>
        </ul>

        <br><br>

        <div class="symptoms-container">
            <form method="POST" action="/goToForm">
                <h1>Symptoms Checking</h1>
                
                <fieldset>
                    <legend>
                        <h4>Are you or anyone you know experiencing any of these symptoms?</h4>
                    </legend>

                    {{#each symptomNames}}

                    <span class="symptom">
                        {{this.description}}
                    </span>

                    <label for="yes">
                        <input type="radio" required name="{{names}}_{{illness_id}}_{{this.id}}"
                            value="yes"><span>Yes</span>
                    </label>

                    <label for="no">
                        <input type="radio" required name="{{names}}_{{illness_id}}_{{this.id}}" checked value="no"><span>No</span>
                    </label>
                    <br>
                    {{/each}}

                    <br>
                    <button type="submit">Submit</button>
                </fieldset>
            </form>
        </div>
        <br><br><br>
    </div>
</body>

</html>